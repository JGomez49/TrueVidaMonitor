
</body>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>

    <script src="https://cdn.jsdelivr.net/npm/chart.js@3.6.0/dist/chart.min.js"></script>

    <script>
        let datosY = [];
        let datosX = [];
        let datosYreset = [];
        let valorReset = "";
        if(document.getElementById('valorReset')){
            valorReset = document.getElementById('valorReset').innerText;
        };
        
        if(document.getElementById('tablacliente')){
            const tabla = document.getElementById('tablacliente');
            const rows = tabla.rows.length;
            const cols = document.getElementById('tablacliente').rows[0].cells.length
            for (let i = 1; i < rows; i++) {
                datosX.push(tabla.rows[i].cells[1].innerHTML);
                datosY.push(tabla.rows[i].cells[2].innerHTML);
                datosYreset.push(valorReset);
            }
        };

        if(document.getElementById('myChart')){
            const ctx = document.getElementById('myChart').getContext('2d');
            const myChart = new Chart(ctx, {

                data: {
                    labels: datosX,
                    datasets: [
                        {
                            type: 'line',
                            label: 'AUA',
                            fill: true,
                            tension: 0.3,
                            data: datosY,
                            backgroundColor: ['rgba(54, 162, 200, 0.5)'],
                            borderWidth: 3
                        },
                        
                        {
                            type: 'line',
                            label: 'Reset',
                            fill: true,
                            tension: 0.3,
                            data: datosYreset,
                            backgroundColor: ['rgba(54, 162, 235, 0.7)'],
                            borderWidth: 3
                        }
                    ]
                },

                options: {
                    interaction: {
                        mode: 'index',
                        intersect: false
                    },
                    //spanGaps: 1000 * 60 * 60 * 24 * 2, // 2 days
                    responsive: true,
                    scales: {
                        x: {
                            // time: {
                            //     tooltipFormat: 'DD T',
                            //     unit: 'day'
                            // },
                            beginAtZero: false,
                            position: 'bottom',
                            display: true,
                            offset: true,
                            //type: 'logarithmic',
                            //min: 10,
                            //max: 50,
                            //suggestedMin: 30,
                            //suggestedMax: 50,
                            // ticks: {
                            //     stepSize: 50
                            // },
                            stacked: false,
                            title: {
                                display: true,
                                text: 'DATE',
                                //color: 'red',
                                // font: {
                                //     family: 'Comic Sans MS',
                                //     size: 20,
                                //     weight: 'bold',
                                //     lineHeight: 1.2,
                                // },
                                // padding: {top: 20, left: 0, right: 0, bottom: 0}
                            },                           
                            legend: {
                                position: 'top',
                            },                          
                            // grid: {
                            //     borderColor: 'blue'
                            // }
                            // ticks: {
                            //     callback: function(val, index) {
                            //         return index % 2 === 0 ? this.getLabelForValue(val) : '';
                            //     },
                            //     color: 'blue',
                            // }
                        },

                        y: {
                            // time: {
                            //     tooltipFormat: 'DD T',
                            //     unit: 'day'
                            // },
                            beginAtZero: true,
                            position: 'left',
                            display: true,
                            offset: true,
                            //type: 'logarithmic',
                            //min: 50000,
                            //max: 300000,
                            //suggestedMin: 30,
                            //suggestedMax: 50,
                            // ticks: {
                            //     stepSize: 10000
                            // },
                            stacked: false,
                            title: {
                                display: true,
                                text: 'AUA',
                                //color: 'blue',
                                // font: {
                                //     family: 'Comic Sans MS',
                                //     size: 20,
                                //     weight: 'bold',
                                //     lineHeight: 1.2,
                                // },
                                // padding: {top: 20, left: 0, right: 0, bottom: 0}
                            },                          
                            legend: {
                                position: 'top',
                            }                            
                            // grid: {
                            //     borderColor: 'blue'
                            // },
                            // ticks: {
                            //     callback: function(val, index) {
                            //         return index % 2 === 0 ? this.getLabelForValue(val) : '';
                            //     },
                            //     color: 'blue',
                            // }
                        }
                    }
                }
            });
        };

    </script>

</html>